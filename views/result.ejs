<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rain Check Result</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h1 class="text-center mb-4"><%= error ? "Error" : "Rain Check Result" %></h1>

    <div class="alert <%= error ? 'alert-danger' : 'alert-success' %>" role="alert">
      <p class="mb-0"><%= message %></p>
    </div>

    <% if (!error) { %>
      <div class="mt-3">
        <p>Temperature: <strong><%= tempMin %>°C</strong> (min) to <strong><%= tempMax %>°C</strong> (max)</p>
        <p>Chance of Rain: <strong><%= rainChance %>%</strong></p>
      </div>
    <% } %>

    <a href="/" class="btn btn-secondary mt-3">Check another location</a>
  </div>
</body>
</html>